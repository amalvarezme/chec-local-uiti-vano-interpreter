# Dependencias del visor del cuaderno 03 (trayectoria y agrupamiento de circuitos).
#
# Solo hacen falta para CONSTRUIR el tablero: una vez construido, servirlo no usa
# mas que la biblioteca estandar de Python. Por eso el entorno de esta aplicacion no
# tiene torch, ni shap, ni jupyter.
#
# scikit-learn entra porque el cuaderno ajusta el K-Means de la nube EN PYTHON y
# embebe el resultado; el navegador no ajusta ningun modelo, solo reordena opacidades.
# geopandas y shapely entran por el mapa de circuitos, que sale de los shapefiles de
# data/GEO.
pandas>=3.0
numpy>=2.4
pyarrow>=24.0
# Version EXACTA, no un minimo. De plotly.py sale el plotly.js que se empaqueta, y los
# cuatro visores estaticos comparten ese archivo en el cache del navegador solo si los
# cuatro producen el MISMO bundle. Con `>=`, instalarlos en semanas distintas da cuatro
# versiones y cada una se descarga aparte. Para subirla, subela en los CUATRO a la vez.
plotly==6.9.0
geopandas>=1.1
shapely>=2.1
scikit-learn>=1.9
ipython>=9.0
