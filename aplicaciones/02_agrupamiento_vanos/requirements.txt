# Dependencias del visor del cuaderno 02 (agrupamiento de vanos por UITI acumulado).
#
# Solo hacen falta para CONSTRUIR el tablero: una vez construido, servirlo no usa
# mas que la biblioteca estandar de Python.
#
# scikit-learn y scipy entran porque el cuaderno ajusta el K-Means y calcula los KDE
# EN PYTHON y embebe el resultado; el navegador no ajusta ningun modelo. geopandas no
# entra: este tablero no dibuja mapa.
pandas>=3.0
numpy>=2.4
pyarrow>=24.0
# Version EXACTA, no un minimo. De plotly.py sale el plotly.js que se empaqueta, y
# la aplicacion 01 y la 02 comparten ese archivo en el cache del navegador solo si
# las dos producen el MISMO bundle. Con `>=`, instalarlas en semanas distintas da dos
# versiones y cada una se descarga aparte. Para subirla, subela en las DOS a la vez.
plotly==6.9.0
scipy>=1.17
scikit-learn>=1.9
ipython>=9.0
