# === Core data & ML ===
numpy
pandas
scipy
scikit-learn
matplotlib
seaborn
tqdm
joblib

# === Deep learning / inference ===
torch

# === Hyperparameter search ===
optuna

# === Visualization ===
plotly
umap-learn  # 2D projection of MGCECDL embeddings -- no in-repo consumer since notebooks 11/12 were removed

# === Data I/O ===
openpyxl
pyarrow

# === PDF extraction ===
PyMuPDF
pdfplumber

# === Weather / external API ===
openmeteo-requests
requests-cache
retry-requests

# === Validation & config ===
python-dotenv
pydantic
jsonschema

# === Jupyter & dev ===
ipython
jupyter
ipykernel
papermill  # parametrized notebook execution -- used by /experimento-kaggle's smoke/full harness
ipywidgets  # 01.5's live-kernel controls (D3); requires anywidget for plotly's go.FigureWidget
anywidget  # backs plotly>=6's go.FigureWidget -- without it FigureWidget() raises ImportError

# === Testing ===
pytest
# Para probar `aplicaciones/databricks/criticidad_chec`, la app consolidada que sirve
# los cuatro tableros en Databricks. Su `app.py` no corre en este entorno -- corre en
# el contenedor de la app --, pero SI se prueba aqui: enrutado, negociacion de gzip y
# cabeceras de cache. Sin estas dos, esa prueba se saltaria, y una prueba que se salta
# en silencio se lee igual que una que pasa.
#
# `databricks-sdk` NO hace falta: la prueba lo sustituye por un doble que lee del
# disco. Es la unica llamada que este entorno no puede hacer de verdad.
fastapi   # la app consolidada de Databricks
httpx     # lo necesita `fastapi.testclient.TestClient`

# === Kaggle experiment CLI ===
kaggle  # used by /experimento-kaggle's auth/config + push/status/output commands

# === Graph Visualization ===
networkx
pyvis

# === Geospatial analysis ===
geopandas
pyogrio
shapely
pyproj
folium
mapclassify

# === Knowledge Graph Skill ===
# `graphify` (src/chec_local_interpreter/graph_extractor.py:build_graphify_context) shells out to
# an external `graphify` CLI binary (a Claude Code skill's own bundled tool, not a PyPI package)
# via subprocess.run(["graphify", ...]). It is optional at runtime: the function degrades
# gracefully to a native PyVis fallback graph when the binary is absent or fails, so it is
# intentionally not pip-installed here.
