---
description: Sube/sincroniza los paquetes fuente (chec_local_interpreter, chec_impacto, scripts) y los 6 cuadernos que corren en Databricks (05 en notebooks/, los otros 5 en notebooks/base_apps/) al mismo Workspace de Databricks usado por /subir-a-databricks, ajustando únicamente sus celdas de arranque para que corran en Databricks sin generar archivos de site/, preguntando solo la URL del workspace destino.
---

> **Read `.claude/commands/_contrato-despliegue-databricks.md` before anything else.** It is mandatory and it overrides what follows:
> - **A. Run log** — open the bitacora *before* asking the user anything, record every numbered step as you finish it, and always close it. Its path and final state are part of the report back to the user.
> - **B. Never abort** — a restriction gets recorded and worked around; the command runs to the end regardless. Wherever this file says "stop and report", rule B applies instead.
> - **C. Unity Catalog target** — `workspace.default.chec-simulador` below is a default, not a requirement. Resolve it at runtime and substitute the resolved value into every path here.
> - **D. Known restrictions** — D1–D9. If one shows up, do not re-diagnose it.

Follow this exact sequence when `/subir-notebooks-databricks` is invoked. It is the standalone, single-source-of-truth command for getting the 6 notebooks that run in Databricks up there — useful on its own when only the notebooks or the source packages changed and neither `data/`, the tables/dashboard, nor the reports need to be touched. `/subir-a-databricks` reuses this exact command by cross-reference instead of duplicating this logic.

**The 6 notebooks, WITH THEIR PATH, and what they were called before.** The folder was renumbered on 2026-08-04 and reorganised on 2026-08-13 — `project_flow/` was dissolved into `notebooks/` and five of these six were archived under `base_apps/`, where they remain the source of the desktop apps. The old numbering survives in older commits and in prose across `docs/` that has not been swept yet.

> **FUERA DE SERVICIO para los tableros 01, 02 y 03 desde el 2026-08-15.** Su codigo ya
> no esta en el cuaderno: vive en `src/chec_tableros/{clima,agrupamiento,trayectorias_circuitos}.py`
> (`sdd/retire-base-apps-notebooks`, fase 3), y el cuaderno quedo como envoltorio delgado.
> El shim de la seccion 4 busca `REPO_ROOT = find_repo_root()` dentro del `.ipynb` para
> reescribirlo, y ahi ya no hay nada que reescribir: subirlos publica un tablero vacio.
>
> Los cuadernos **05 y 06 siguen siendo validos** y este comando los sigue cubriendo. Es
> el aviso el que va acotado a 01-03, no el comando entero. La rebanada S13 lo reescribe
> alrededor del contrato de tres artefactos.

| Path | Was | What it is |
|---|---|---|
| `notebooks/base_apps/01_uiti_vano_clima.ipynb` | `01.6` | **migrado a `src/`** — subirlo ya no publica el tablero |
| `notebooks/base_apps/02_uiti_vano_kmeans.ipynb` | `01.2` | **migrado a `src/`** — subirlo ya no publica el tablero |
| `notebooks/base_apps/03_uiti_vano_trayectorias_circuitos.ipynb` | `01.3` | **migrado a `src/`** — subirlo ya no publica el tablero |
| `notebooks/base_apps/04_uiti_vano_trayectorias_vano.ipynb` | `01.4` | Vano-level trajectories + map; **owns the KMeans geometry 05 and 06 replay** |
| `notebooks/05_mil_vano_ventana.ipynb` | `10` | MIL over vano × window bags (generated by `scripts/generate_notebook_10.py`) |
| `notebooks/base_apps/06_uiti_vano_explicabilidad_simulador.ipynb` | `01.5` | Live-kernel explainability + risk simulator |

**Enumerate those six paths explicitly. Never glob a folder.** Since the 2026-08-14 cleanup deleted the 8 MGCECDL notebooks, `base_apps/` happens to hold exactly these five and `notebooks/` exactly `05` — so a glob would *currently* land on the right set. Do not lean on that: it is a coincidence of today's contents, not a rule, and the whole point of the explicit list is that adding a sixth notebook to either folder must be a deliberate edit here rather than something a glob silently sweeps up. The folder held thirteen until yesterday, and the glob that used to be here (`notebooks/*.ipynb`, non-recursive) silently skipped five of the six.

**Out of scope**: this command MUST NOT upload `data/`, MUST NOT create/refresh any Delta table or view, MUST NOT run any of the uploaded notebooks as a job, and MUST NOT touch the Lakeview dashboard or the interpretability reports. **It also MUST NOT cause any notebook copy to write into a `site/`-named path anywhere in the Volume** — the project's `site/` tree (its HTML/PNG assets, published separately via GitHub Actions/Pages) is only ever regenerated by a local run against the real repo paths. None of the 6 notebooks resolves a `site/...` path at all (that was a property of `04_mgcecdl_performance` and `07_graph_preserved_connections`, both deleted from the tree on 2026-08-14), so here the rule is satisfied by not reintroducing one — step 4's audit table records that explicitly rather than leaving it unstated.

## 0. Ask the user for the one required input

Ask, and wait for the answer:
1. The Databricks workspace URL (e.g. `https://dbc-xxxxxxxx-xxxx.cloud.databricks.com`).

Do not ask about profile up front — resolve it automatically below. No warehouse is needed — this command only imports files, it never runs SQL or submits a job.

## 1. Resolve profile and user identity (reuse, do not re-derive)

Follow `.claude/commands/_contrato-despliegue-databricks.md` **section E1** (resolve the CLI profile) verbatim, using the workspace URL from step 0. Carry forward the resolved `<profile>` for every command below.

- Matching profile found → use it.
- No matching profile → tell the user to run `databricks auth login --host <workspace-url>` via the `!` prefix (interactive OAuth, cannot be run for them), then re-resolve.

Also get the profile's own user identity, needed for every Workspace path below:
```
databricks current-user me -p <profile>
```
Use the returned `userName` in place of `<userName>` everywhere in this document.

## 2. Check the data prerequisite exists (do not re-upload it here)

The notebooks read from `dbfs:/Volumes/workspace/default/chec-simulador/data`. Check it's populated:
```
databricks fs ls dbfs:/Volumes/workspace/default/chec-simulador/data -p <profile>
```
Needs at minimum `Indicadores_vano_v3.csv` (all 6) and `GEO/MVLINSEC.shp` (01, 03, 04, 06). If any are missing, tell the user to run `/subir-datos-databricks` first — do not re-implement that upload here.

Two more prerequisites belong to `05`/`06` specifically and are easy to miss because they are not what those notebooks *read first*:

- `models/mgcecdl_classifier_best.zip` (`06`'s SEAM cell) and `models/mil_vano_ventana_v1.pt` (`05`'s viewer mode, which is its **default** — it only retrains when `ENTRENAR` is set).
- `geometria_kmeans_014_v1.json`. Both notebooks replay the KMeans geometry through `chec_local_interpreter.ventanas_015.cargar_clases_criticidad`. **This one stopped being a prerequisite you have to prepare**: since 2026-08-15 the file is tracked in git at `data/geometria_kmeans_014_v1.json` (310 bytes), so a fresh checkout already has it and `/subir-datos-databricks` carries it with the rest of `data/`. The old failure mode is gone with it — there is no lazy regeneration left that could try to parse the `04` notebook, which in Databricks is a Workspace object rather than a file on disk. If you ever need to rebuild it, `python scripts/exportar_geometria.py` refits it from the CSV.

## 3. Verbatim import of the three source roots

Create the parent Workspace folder first — defensive, since `import-dir` has been observed to create its own destination tree on its own, but that behavior isn't documented API contract, only an empirical observation from one run; `mkdirs` is idempotent (no error if the folder already exists), so there's no cost to being explicit:
```
databricks workspace mkdirs /Workspace/Users/<userName>/databricks-integration -p <profile>
```

Use `import-dir --overwrite` for `chec_local_interpreter` and add `chec_impacto` and `scripts` (which that command does not upload):

```
databricks workspace import-dir src/chec_local_interpreter /Workspace/Users/<userName>/databricks-integration/chec_local_interpreter_src/chec_local_interpreter --overwrite -p <profile>
databricks workspace import-dir src/chec_impacto /Workspace/Users/<userName>/databricks-integration/chec_impacto_src/chec_impacto --overwrite -p <profile>
databricks workspace import-dir scripts /Workspace/Users/<userName>/databricks-integration/scripts_src/scripts --overwrite -p <profile>
```

**`scripts/` used to be a mandatory third source root; since 2026-08-15 it is not.** The reason it was mandatory was that `src/chec_local_interpreter/ventanas_015.py` did `from scripts.extract_geometrias_014 import ...` at **module import time**, so `05` and `06` failed on the import itself with `ModuleNotFoundError: No module named 'scripts'` rather than late at the geometry step. That import is gone along with the module — verified: nothing under `src/` or in either notebook imports the `scripts` package any more, only mentions it in prose.

The upload and its `sys.path` entry are kept below because they are harmless and cost one call, and because dropping them is a deploy-behaviour change that has not been exercised against a live workspace. Treat them as optional, not as a prerequisite: their absence no longer explains an import failure.

If `import-dir` rejects a non-notebook file in any of the three, fall back to individual `databricks workspace import <target_path> --file <local_file> --language PYTHON --format RAW --overwrite -p <profile>` calls for the rejected files.

`import-dir` has no exclude/filter mechanism — if any of the three local package trees has `__pycache__/*.pyc` from local runs, they get uploaded too (confirmed empirically: a run against a real workspace uploaded 106 stale `.pyc` files across the two `src/` packages this way; `scripts/` has its own `__pycache__` too). Clean them up right after the imports — do not leave them in the Workspace:
```
databricks workspace delete --recursive /Workspace/Users/<userName>/databricks-integration/chec_local_interpreter_src/chec_local_interpreter/__pycache__ -p <profile>
databricks workspace delete --recursive /Workspace/Users/<userName>/databricks-integration/chec_impacto_src/chec_impacto/__pycache__ -p <profile>
databricks workspace delete --recursive /Workspace/Users/<userName>/databricks-integration/scripts_src/scripts/__pycache__ -p <profile>
```
Both `src/` packages have `__pycache__` nested under multiple subpackages, not just at the root — find every one locally first (`find src/chec_local_interpreter src/chec_impacto scripts -type d -name __pycache__`) and repeat the delete for each match; deleting only the top-level `__pycache__` leaves the nested ones behind.

Also upload `requirements.txt` next to the two package roots, so the notebooks in step 4 can `%pip install -r` it from the Volume-relative bootstrap — but upload a **trimmed copy**, not the repo file verbatim. Verified by statically diffing every third-party top-level import across `src/chec_impacto` and `src/chec_local_interpreter` against `requirements.txt`: the local-dev/test-only entries `jupyter`, `ipykernel`, `pytest`, `python-dotenv`, `pydantic` are never imported by either package or by any of the 6 active notebooks (Databricks already provides its own notebook kernel, so `jupyter`/`ipykernel` would be actively redundant there; `pytest` only runs the test suite, never imported at runtime; `python-dotenv`/`pydantic` have zero references anywhere in this repo). Everything else in `requirements.txt` IS reachable from `src/` (confirmed via AST import-graph audit, 2026-07-24) — do not trim further than this without re-running that audit, since guessing which packages a given notebook "probably" doesn't need is exactly the kind of assumption that caused the `optuna` `ModuleNotFoundError` in the first place.
```
grep -vE '^(jupyter|ipykernel|pytest|python-dotenv|pydantic)$' requirements.txt > /tmp/requirements-databricks.txt
databricks workspace import /Workspace/Users/<userName>/databricks-integration/requirements.txt --file /tmp/requirements-databricks.txt --language PYTHON --format RAW --overwrite -p <profile>
rm -f /tmp/requirements-databricks.txt
```

## 4. Notebook bootstrap shim (staged copies only — never touch the originals)

**Hard invariant**: every notebook uploaded in this step is a modified COPY prepared in a scratch location (e.g. `/private/tmp/.../scratchpad/`), never the file under `notebooks/` in this repo. Only the notebook's bootstrap cell(s) — the code that computes where the repo/data/`src` live — is rewritten in the copy. All other cells (markdown, analysis, outputs) stay byte-identical to the repo original. After this step finishes, `git status --porcelain notebooks/` in this repo MUST show zero changes — if it does not, something wrote to the wrong path; stop and fix that before continuing.

**Strip cell outputs from every staged copy before upload.** `databricks workspace import --format JUPYTER` enforces a 10MB payload limit, and for this set that is not an edge case — it is the norm. Measured on the current files:

| Notebook | With outputs | Stripped |
|---|---|---|
| `01_uiti_vano_clima` | **81.43 MB** | 0.05 MB |
| `02_uiti_vano_kmeans` | **9.39 MB** | 0.08 MB |
| `03_uiti_vano_trayectorias_circuitos` | **11.58 MB** | 0.09 MB |
| `04_uiti_vano_trayectorias_vano` | **12.31 MB** | 0.09 MB |
| `05_mil_vano_ventana` | 0.07 MB | 0.06 MB |
| `06_uiti_vano_explicabilidad_simulador` | 0.06 MB | 0.05 MB |

Three of the six are over the ceiling outright and `01` is 8× it — those four embed a full interactive Plotly panel as `text/html` in their last cell. Clear `outputs` and `execution_count` on every code cell of every staged copy; this discards stale rendering from a prior local run, never source code, and it regenerates on the first real run in Databricks. Do it for all 6 as a standard step, not only the ones currently over the limit.

The 6 notebooks fall into 3 bootstrap variants.

**General rule (applies to all 3 variants)**: the shim replaces ONLY the root-resolution logic — the `find_repo_root()` / `resolve_project_root()` function + its call (Variants A and B), or the `Path.cwd()` walk-up loop (Variant C) — plus the `sys.path`-insertion block that immediately follows it, where there is one. **Every other line already present in that same bootstrap cell is left completely unchanged**, and so is every later cell that references the root variable or any name derived from it. This works because the replacement always **aliases the exact same root-variable name the notebook already used** (`REPO_ROOT` for 01-04, `PROJECT_ROOT` for 05, `ROOT` for 06) to `DATA_DIR.parent` — so any existing `<root_var> / "data" / ...` expression still resolves to `DATA_DIR / ...`, and `03`/`04`'s `<root_var> / "reports" / "reportescircuitos" / "artifacts"` still resolves to a Volume `reports/` subfolder, without hunting down every downstream reference.

Canonical replacement block (swap in for the root-resolution + sys.path portion only; `<root_var>` is the notebook's own name per the per-variant note below):
```python
DATA_DIR = Path("/Volumes/workspace/default/chec-simulador/data")
<root_var> = DATA_DIR.parent
# --- only for 05 and 06, the two that import the local packages ---
for _p in ("/Workspace/Users/<userName>/databricks-integration/chec_impacto_src",
           "/Workspace/Users/<userName>/databricks-integration/chec_local_interpreter_src",
           "/Workspace/Users/<userName>/databricks-integration/scripts_src"):
    if _p not in sys.path:
        sys.path.insert(0, _p)
```
The `scripts_src` entry is the one that is easy to drop and expensive to debug — see section 3.

**Every staged copy also needs its own dependency install — do not skip this even though the general rule above only touches the root-resolution logic.** The local notebooks assume a pre-configured local Python environment (`pip install -r requirements.txt` run once, outside the notebook); that assumption does not carry over to Databricks, where each notebook run gets a fresh environment. The failure is transitive and therefore unguessable from the notebook's own imports: the now-deleted `09_simulador` failed with `ModuleNotFoundError: No module named 'optuna'` while never mentioning Optuna itself — it imported `chec_impacto.training`, whose `__init__.py` imports `mgcecdl.py`, which does `import optuna` at module level. `05` and `06` import that same package and inherit exactly this risk.

Fix: insert a NEW first cell — literally `nb["cells"].insert(0, ...)`, before the existing markdown title cell — in every one of the 6 staged copies:
```python
%pip install -q -r /Workspace/Users/<userName>/databricks-integration/requirements.txt
```
`%pip install` triggers an automatic Python interpreter restart in Databricks, which is exactly why it must be the very first cell — inserted before, never after, any cell whose state you need to keep. This applies uniformly to all 6, including `01`-`04`, which import no local package but do need `plotly`, `geopandas`, `scikit-learn` and `scipy`.

**A version floor on `plotly` is mandatory for `01`, `03`, `04` and `06`.** All four build their map with `go.Scattermap` and a `{'type': 'map'}` subplot spec — the MapLibre trace family that only exists in modern Plotly. The Databricks Runtime preinstalls an older Plotly, so a bare `plotly` requirement is **already satisfied** and pip upgrades nothing; the failure then surfaces as `ValueError: Unsupported subplot type: 'map'` pointing at the *system* site-packages. `/app-vano-clima`, `/app-trayectorias-circuitos` and `/app-trayectorias-vanos` document the same floor for the same reason — keep them consistent.

**Variant A — `find_repo_root()` over the data file (`01`, `02`, `03`, `04`)**: `<root_var>` = `REPO_ROOT`. All four define the same small helper, which walks up from `Path.cwd()` looking for `data/Indicadores_vano_v3.csv`, and call it as `REPO_ROOT = find_repo_root()`. **None of the four imports `chec_impacto` or `chec_local_interpreter`** — the only mentions of those packages in `02`/`03`/`04` are comments citing where a join rule came from, which is exactly the kind of thing a grep mistakes for an import. So **omit the `sys.path` block entirely** for these four: replace the function definition and its call with the two `DATA_DIR`/`REPO_ROOT` lines and nothing else. Everything else in that cell (`VARS`, `NOMBRES_GRUPOS`, `COLORES_GRUPOS`, `SEMILLA`, the notebook parameters) is untouched and keeps working through the alias.

**Variant B — `resolve_project_root()` over `src/chec_impacto` + `data` (`05`)**: `<root_var>` = `PROJECT_ROOT`. Its bootstrap cell defines `resolve_project_root()` (walks up for a directory holding both `src/chec_impacto/` and `data/`), then `SRC_DIR`, `DATA_DIR`, `DERIVED_DIR`, a `for path_to_add in (PROJECT_ROOT, SRC_DIR)` sys.path loop, and `DERIVED_DIR.mkdir(...)`. Replace the function + its call + that sys.path loop with the canonical block, and **keep `SRC_DIR`, `DERIVED_DIR` and the `mkdir` line** — `DERIVED_DIR` is where its viewer mode looks for the optional OOF `.npz`, and dropping the `mkdir` makes the training path fail on write. Note `DATA_DIR` is defined by both the original cell and the canonical block; let the canonical block's definition win (put it first and delete the original line) rather than leaving two.

**Variant C — `Path.cwd()` walk for `src/` (`06`)**: `<root_var>` = `ROOT`. Replace the `ROOT = Path.cwd().resolve()` walk-up loop and the `for _path_a_agregar in (ROOT, ROOT / 'src')` sys.path block with the canonical block. **Leave the `sys.modules` purge loop and the `_sonda`/`assert hasattr(_sonda, 'caja')` probe that follow them completely untouched** — the purge must still run before the project imports (it is what makes a re-run pick up freshly uploaded package code instead of the copy already cached in the kernel, which is the same failure mode it was added for locally), and the probe is a cheap first-cell contract check.

**`06` writes nothing by default, in Databricks or locally.** The `ipywidgets` app IS its interface everywhere: circuit, window, vano selection, simulator knobs and the "Simular" button all live in the notebook. Running it produces no files and opens no browser, so this command has nothing extra to redirect.

**`06` has no HTML export path any more.** It used to be able to write a self-contained
`reports/paneles/06_*.html` behind `EXPORTAR_PANEL_WEB`, carrying a working "Simular" with
the MIL forward pass transcribed into JavaScript. That panel was removed on 2026-08-09
together with `chec_local_interpreter.mil_web_export` and its test: it was a second
implementation of the model that had to be kept in step with the Python by hand, and it
fell behind on every change. The notebook is now the only interface, in Databricks and
locally alike, so there is nothing extra to redirect and no `EXPORTAR_PANEL_WEB`,
`EN_DATABRICKS` or `CIRCUITOS_SIMULABLES` left to reason about. There is still no `/app-*`
sibling for `06` — set expectations accordingly when reporting back.

### Per-notebook bootstrap audit

Every row was re-derived from the current files on 2026-08-04, not carried over from the previous numbering.

| Notebook | Variant | Root var | Imports local pkgs? | `sys.path` block? | Other names the bootstrap cell defines that a *later* cell uses | Writes a `site/...` path? |
|---|---|---|---|---|---|---|
| `01_uiti_vano_clima` | A | `REPO_ROOT` | No | No | `VARS`, `NOMBRES_VARS`, `VENTANA_HORAS`, `LAG_MAX`, `VARIABLE_CLIMA`; `REPO_ROOT` itself (the `data/GEO/*.shp` reads) | No |
| `02_uiti_vano_kmeans` | A | `REPO_ROOT` | No (comment mentions only) | No | `NOMBRES_GRUPOS`, `COLORES_GRUPOS`, `PREPROCESOS`, `GRID_KDE`, `SEMILLA`; `REPO_ROOT` (the CSV read) | No |
| `03_uiti_vano_trayectorias_circuitos` | A | `REPO_ROOT` | No (comment mentions only) | No | palette/opacity constants; `REPO_ROOT` (CSV, shapefiles, and the `reports/reportescircuitos/artifacts/` CSV it writes) | No |
| `04_uiti_vano_trayectorias_vano` | A | `REPO_ROOT` | No | No | `MAX_VANOS_RESALTADOS`, `COLORES_VANOS`, palette constants; `REPO_ROOT` (CSV, shapefiles, `reports/.../uiti_vano_ventanas.csv`) | No |
| `05_mil_vano_ventana` | B | `PROJECT_ROOT` | `chec_impacto`, `scripts` | **Yes** | `SRC_DIR`, `DATA_DIR`, `DERIVED_DIR` (+ its `mkdir`); `PROJECT_ROOT` (model and OOF paths) | No |
| `06_uiti_vano_explicabilidad_simulador` | C | `ROOT` | `chec_impacto`, `chec_local_interpreter`, `scripts` | **Yes** | `ROOT` (cell 4's `DATA_PATH`, `VARIABLES_SELECCION_PATH`, `MODEL_DIR`; cell 6's shapefile reads); the `sys.modules` purge and the `.caja` probe must survive verbatim | No |

The `site/...` column is all "No" by design, and that is a change from the previous set: the two notebooks that resolved such a path (`04_mgcecdl_performance`'s `SITE_RESULTS_DIR`, `07_graph_preserved_connections`'s HTML outputs) were deleted from the tree on 2026-08-14. If a future notebook reintroduces one, it needs its own redirect rule here before it can be uploaded.

Create the target Workspace folder first — `workspace import` does NOT auto-create parent directories (confirmed empirically: importing straight into `.../databricks-integration/project_flow/<name>` on a workspace where that folder didn't exist yet failed with "The parent folder ... does not exist"):
```
databricks workspace mkdirs /Workspace/Users/<userName>/databricks-integration/project_flow -p <profile>
```

Upload all 6 staged copies (format `JUPYTER`, matching each file's `.ipynb` content):
```
databricks workspace import /Workspace/Users/<userName>/databricks-integration/project_flow/<notebook_name> --file <staged_copy_path> --format JUPYTER --overwrite -p <profile>
```
Repeat once per notebook, over the six paths in the table above (never a glob). After the loop, run the invariant check:
```
git status --porcelain notebooks/
```
Confirm the output is empty before moving on.

## 5. Report back

Tell the user, in their language:
- **The bitacora**: its path under `reports/despliegues/`, the final state `cerrar` printed (`COMPLETO`, `COMPLETO CON RESTRICCIONES` or `INCOMPLETO`), and the count of restrictions it holds. Do not soften that state in prose.
- **Every restriction recorded, with who unblocks each one** — reproduce the `resumen` output. A run that ended INCOMPLETO reports what is still blocking, not just what worked.
- The profile and workspace this run used.
- That the three source roots (`chec_local_interpreter`, `chec_impacto`, `scripts`) and `requirements.txt` were uploaded (or refreshed) at `/Workspace/Users/<userName>/databricks-integration/...`.
- Which of the 6 notebooks were uploaded, and that each is a shimmed copy — the originals under `notebooks/` were never modified. Say explicitly that the 8 MGCECDL notebooks under `notebooks/base_apps/` were not uploaded.
- Confirmation that `git status --porcelain notebooks/` was empty at the end of step 4.
- That no `site`-named path was created in the Volume: none of the 6 active notebooks resolves one (see the audit table).
- That every one of the 6 staged copies got its own `%pip install -q -r requirements.txt` first cell, so the first real run of each notebook in Databricks installs its dependencies instead of failing with a `ModuleNotFoundError` for whatever package the local environment silently already had.
- That this run did not execute any notebook, touch any table, — if the notebooks also need their data, point to `/subir-datos-databricks` or `/subir-a-databricks`.
- **How to actually run one of these 6 notebooks afterward**: in the Databricks UI, attach it to compute — NOT a SQL Warehouse. A SQL Warehouse can only execute SQL cells; attaching one to a `project_flow` notebook fails immediately on the first cell (confirmed empirically: `Unsupported cell during execution. SQL warehouses only support executing SQL cells.`, thrown on the `%pip install` cell). SQL Warehouses are only for the dashboard/table-building SQL this command family already runs through the CLI — never for these notebooks.
- **`06_uiti_vano_explicabilidad_simulador.ipynb` specifically needs a general-purpose (classic, "all-purpose") cluster, NOT Serverless.** Its whole panel is `ipywidgets` (the selector, the sliders, the "Simular" button). Databricks' own documentation states plainly: "A notebook using ipywidgets must be attached to a running cluster" — Serverless compute is excluded (confirmed via Databricks docs, 2026-07-24, after a live run of the since-deleted `09_simulador` where the final widget silently failed to render on Serverless with no error at all). Databricks Runtime 13.0+ is required for ipywidgets GA support (11.0-12.2 LTS only had it in preview). The other 5 notebooks don't use ipywidgets and can still run on Serverless.
- **`06` also needs a warm kernel to be worth anything**: it is the only one of the 6 with no exportable artifact. `02`, `03` and `04` each render a standalone HTML panel and have their own `/app-*` command that serves it at a stable URL; `01` renders its panel inline; `05` prints its evaluation. `06` renders nothing without a live kernel — do not promise the user a URL for it.
